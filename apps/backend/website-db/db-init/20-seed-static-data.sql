-- Static Data Seeding Script
-- This script inserts any necessary static data into the database

-- Connect to the database
\c :POSTGRES_DB

-- Example: Insert default configuration values
-- INSERT INTO configurations (key, value) VALUES
-- ('default_theme', 'light'),
-- ('max_contact_attempts', '3');

-- Note: Add any static data that needs to be pre-populated in the database 